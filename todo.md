# Todo

## 1、Bewly宽屏右侧窗口显示问题
### 详情
当我在Bewly宽屏模式的右侧窗口下，比如“首页”中向下滚动了页面，然后点击“评论”标签，再点击“首页”，此时“首页”会显示最上面的内容，
而非点击“评论”之前的内容。
### 完成
已完美修复

## 2、Bewly宽屏切换问题
### 详情
当我从Bewly宽屏模式切换到B站官方的全屏模式后，再退出全屏模式，此时会回到普通模式，而非Bewly宽屏模式。
从Bewly宽屏模式切换到其他模式，比如B站的宽屏模式、网页全屏模式时也会有这种现象。
### 完成

## 3、Bewly宽屏模式动画问题
### 详情
当在Bewly宽屏模式下通过“首页”标签跳转到其他视频页时，会经历一段从B站默认模式到Bewly宽屏模式的转换过程，
能否跳过这个过程，直接呈现Bewly宽屏模式？
### 完成
但仍可优化加载速。已由4、Bewly宽屏模式切换动画完成。

## 4、Bewly宽屏模式切换动画
### 详情
当我在Bewly宽屏模式点击右侧窗口“首页”中的视频后，整个页面会突然变成白色，显示一行字“正在加载宽屏模式”，
然后再过一会儿才进入完整的宽屏模式。会给用户一种页面闪烁的感觉，体验不好。需要优化为在Bewly宽屏模式下点击右侧窗口
“首页”中的视频时，整个页面的框架保持不变，只有页面左侧的视频播放窗口切换视频，达成一种在当前页面切换视频的流程体验。

### 完成
已改为页面内原地换片：复用 B 站视频页自身的 handleVideoRoute + switchVideo，
不重载文档，宽屏外壳、首页列表与滚动位置全部保留，换片期间用新视频封面盖住播放器。
评论区延后到点击「评论」标签时才切换；弹幕与推荐由 B 站自身原地重渲染，无法冻结。
番剧/直播卡片、缺少 aid 的卡片以及注入脚本失效时，仍回退到原有的整页跳转。

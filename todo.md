# Todo

## 1、Bewly宽屏右侧窗口显示问题
当我在Bewly宽屏模式的右侧窗口下，比如“首页”中向下滚动了页面，然后点击“评论”标签，再点击“首页”，此时“首页”会显示最上面的内容，
而非点击“评论”之前的内容。
### 完成
已完美修复

## 2、Bewly宽屏切换问题
当我从Bewly宽屏模式切换到B站官方的全屏模式后，再退出全屏模式，此时会回到普通模式，而非Bewly宽屏模式。
从Bewly宽屏模式切换到其他模式，比如B站的宽屏模式、网页全屏模式时也会有这种现象。

## 3、Bewly宽屏模式动画问题
### 详情
当在Bewly宽屏模式下通过“首页”标签跳转到其他视频页时，会经历一段从B站默认模式到Bewly宽屏模式的转换过程，
能否跳过这个过程，直接呈现Bewly宽屏模式？
### 完成
但仍可优化加载速。
